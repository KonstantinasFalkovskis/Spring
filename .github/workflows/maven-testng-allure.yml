name: Java CI with Maven

on:
  #  schedule:
  #    - cron: '00 18 * * *'
  push:
    branches: [ main ]

#refactoring!
#defaults:
#  run:
#    shell: bash

#jobs:
#  maven-save:
#    runs-on: ${{ matrix.os }}
#    permissions:
#      contents: write
#    concurrency:
#      group: ${{ github.workflow }}-${{ github.ref }}
#    strategy:
#      fail-fast: false
#      matrix:
#        os: [ubuntu-latest]
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v3
#      - name: Set up JDK 11
#        uses: actions/setup-java@v3
#        with:
#          java-version: '11'
#          distribution: 'temurin'
#          cache: maven
#
#      - name: Create files to cache
#        run: |
#          mvn verify -f __tests__/cache/maven/pom.xml
#          if [ ! -d ~/.m2/repository ]; then
#            echo "::error::The ~/.m2/repository directory does not exist unexpectedly"
#            exit 1
#          fi
#  maven-restore:
#    runs-on: ${{ matrix.os }}
#    permissions:
#      contents: write
#    concurrency:
#      group: ${{ github.workflow }}-${{ github.ref }}
#    strategy:
#      fail-fast: false
#      matrix:
#        os: [ubuntu-latest]
#    needs: maven-save
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v3
#      - name: Set up JDK 11
#        uses: actions/setup-java@v3
#        with:
#          java-version: '11'
#          distribution: 'temurin'
#          cache: maven
#
#      - name: Confirm that ~/.m2/repository directory has been made
#        run: |
#          if [ ! -d ~/.m2/repository ]; then
#            echo "::error::The ~/.m2/repository directory does not exist unexpectedly"
#            exit 1
#          fi
#          ls ~/.m2/repository
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}

    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: maven

      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-m2-
      - run: mvn verify -DskipTests

      - name: Build with Maven
        if: always()
        run: mvn clean test -Dsurefire.suiteXmlFiles=./src/test/resources/suites/frontend/e2e.xml -Dspring.profiles.active=stage
        continue-on-error: true

      - name: Get Allure history
        uses: actions/checkout@v3
        if: always()
        continue-on-error: true
        with:
          ref: gh-pages
          path: gh-pages

      - name: Allure Report action from marketplace
        uses: simple-elf/allure-report-action@master
        if: always()
        id: allure-report
        with:
          allure_results: target/allure-results
          gh_pages: gh-pages
          allure_report: allure-report
          allure_history: allure-history
          keep_reports: 20

      - name: Deploy report to Github Pages
        if: always()
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: allure-history